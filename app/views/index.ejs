<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= headerTitle %></title>
  <style>
    body{font-family:system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif;max-width:900px;margin:3rem auto;padding:0 1rem;line-height:1.55}
    header{display:flex;flex-direction:column;gap:.25rem;margin-bottom:1rem}
    h1{font-size:2rem;margin:0}
    .name{font-weight:700;font-size:1.125rem;color:#555}
    ul{margin-top:.5rem;padding-left:1.25rem}
    li{margin:.5rem 0}
    .term{font-weight:600}
  </style>
</head>
<body>
  <!-- Your name at the top -->
  <header>
    <div class="name"><%= yourName %></div>
    <h1><%= headerTitle %></h1>
  </header>

  <!-- List of MEAN definitions -->
  <ul>
    <% definitions.forEach(item => { %>
      <li>
        <span class="term"><%= item.term %>:</span>
        <span class="def"><%= item.definition %></span>
      </li>
    <% }) %>
  </ul>
</body>
</html>
